# Auto-generated by .github/workflows/release.yml — do not edit by hand.
class ${CLASS_NAME} < Formula
  desc "Rust CLI demo that encrypts and decrypts sensitive fields via the Evervault REST API"
  homepage "https://github.com/${GITHUB_REPOSITORY}"
  version "${FORMULA_VERSION}"
  license "MIT"

  on_macos do
    if Hardware::CPU.arm?
      url "https://github.com/${GITHUB_REPOSITORY}/releases/download/v${FORMULA_VERSION}/evervault-rs-${FORMULA_VERSION}-aarch64-apple-darwin.tar.gz"
      sha256 "${DARWIN_ARM64_SHA}"
    else
      url "https://github.com/${GITHUB_REPOSITORY}/releases/download/v${FORMULA_VERSION}/evervault-rs-${FORMULA_VERSION}-x86_64-apple-darwin.tar.gz"
      sha256 "${DARWIN_X86_64_SHA}"
    end
  end

  on_linux do
    if Hardware::CPU.arm?
      url "https://github.com/${GITHUB_REPOSITORY}/releases/download/v${FORMULA_VERSION}/evervault-rs-${FORMULA_VERSION}-aarch64-unknown-linux-gnu.tar.gz"
      sha256 "${LINUX_ARM64_SHA}"
    else
      url "https://github.com/${GITHUB_REPOSITORY}/releases/download/v${FORMULA_VERSION}/evervault-rs-${FORMULA_VERSION}-x86_64-unknown-linux-gnu.tar.gz"
      sha256 "${LINUX_X86_64_SHA}"
    end
  end

  def install
    bin.install "evervault-rs"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/evervault-rs --version")
  end
end
